<div class="overlay">
	<p class="counter"></p>
	<p class="loading-bar"></p>
	<p class="message px-40">
		“Every purhase you make from WAP goes toanimals welfare and [anything good that happens when a user buys from WAP]"
	</p>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div>
	<div class="bar"></div> 
</div>

<style>
div.bar:empty, p:empty {
	display: block;
}
.overlay {
	position: fixed;
	width: 100vw;
	height: 100vh;
	z-index: 5;
	display: flex;
}
.bar {
	width: 10vw;
	height: 100vh;
	background-color: #000000;
}
.counter {
	position: absolute;
	bottom: 10%;
	width: 100%;
	height: 10%;
	right: 5%;
	display: flex;
	justify-content: flex-end;
	align-items: center;
	color: #000000;
	font-size: 5vw;
	z-index: 6;
	margin:0;
}
.loading-bar {
	position: absolute;
	bottom: 0;
	width: 100%;
	height: 3%;
	display: flex;
	justify-content: flex-end;
	align-items: center;
	color: #000000;
	font-size: 5vw;
	z-index: 6;
	background-color: #da1313;
	margin:0;
}
.message {
	position: absolute;
	bottom: 0;
	font-size: 2.25rem;
	left: 0;
	width: 100%;
	height: 100%;
	color: #000000;
	display: flex;
	background: #fdfdf3;
	justify-content: center;
	align-items: center;
	margin:0;
}
</style>

<script>
    function startLoader() {
	let loadingBar = document.querySelector(".loading-bar");
	let counterElement= document.querySelector(".counter");
	let currentValue = 0;

	function updateCounter() {
		if (currentValue === 100) {
			return;
		}

		currentValue += Math.floor(Math.random() * 10 + 1);

		if (currentValue > 100) {
			currentValue = 100;
		}

		counterElement.innerHTML = `${currentValue}%`;
		loadingBar.style.width = `${currentValue}%`;
		let delay = Math.floor(Math.random() * 100) + 50;

		setTimeout(updateCounter, delay);
	}
	updateCounter();
}

startLoader();

gsap.to(".loading-bar", 1, {
	delay: 1.5,
	display: "none"
});

gsap.to(".overlay", 1.5, {
	delay: 1.55,
	display: "none"
});

gsap.to(".message", 0.25, {
	delay: 1.5,
	display: "none"
});

gsap.to(".counter", 1, {
	delay: 1.5,
	display: "none"
});

gsap.to(".bar", 1.5, {
	delay: 1.5,
	height: 0,
	stagger: {
		amount: 0.5
	},
	ease: "power4.inOut"
});

gsap.to("body", 0.2, {
	delay: 0.2,
	opacity: 1
});

</script>